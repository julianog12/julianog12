<%= form_for @diff, :url => {:action => :create}  do |f| %>
  <div class="form-group shadow-textarea">
    <%= label_tag "Texto Antigo" %>
    <%= f.text_area :before, :class => "form-control z-depth-1", :rows => 12 %>
  </div>
  <div class="form-group shadow-textarea">
    <%= label_tag "Texto Novo", nil %>
    <%= f.text_area :after, :class => "form-control z-depth-1", :rows => 12 %>
  </div>
  <div class="actions">
     <%= submit_tag "Mostrar Diferença", class: "btn btn-primary btn-block" %>
  </div>
<% end %>
